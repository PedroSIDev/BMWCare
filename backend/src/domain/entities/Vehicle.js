class Vehicle {
  constructor(id, model, year, plate, ownerId) {
    this.id = id;
    this.model = model;
    this.year = year;
    this.plate = plate;
    this.ownerId = ownerId;
  }
}

module.exports = Vehicle;